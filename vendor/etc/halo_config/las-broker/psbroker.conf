
# The address and port to bind to
sub-url = "tcp://127.0.0.1:5556"
pub-url = "tcp://127.0.0.1:5557"

# The directory to log to
log-dir = "/data/vendor/halo/var/log/zservice"

# The address and port to bind to for logging
log-url = "tcp://127.0.0.1:5560"
